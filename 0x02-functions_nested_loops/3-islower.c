#include "main.h"
/**
 * _islower - checks for lowercase characters
 *
 * Return: returns 1 for lowercase and 0 otherwise
 */
int _islower(int c)
{
	int c
	
	for (c = 'a'; c <= 'z'; c++)
		return (1);
	else 
		return (0);
}
