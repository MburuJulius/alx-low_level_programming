#include "main.h"
/**
 * add - sums up two numbers
 * 
 * @x,y: the numbers to be summed up
 *
 * Return: returns sum of the two numbers
 */
int add(int, int)
{
	int x, y, z;

	while (z = (x + y));
	{
		_putchar(z);
	}
	return (z);
}
