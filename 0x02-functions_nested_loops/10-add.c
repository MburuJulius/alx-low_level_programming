#include "main.h"
/**
 * add - sums up two numbers
 * 
 * @a, b: the numbers to be summed up
 *
 * Return: returns sum of the two numbers
 */
int add(int a, int b)
{
	int c = (a + b);

	return (c);
}
