#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 *
 * Return: Always 0 
 */
int _abs(int)
{
	int v;

	if (v >= 0)
	{
		return (v);
	}
	else if (v < 0)
	{
		return (v * -1);
	}
}
