#include <stdio.h>
/**
 * main - prints single numbers separated by comma and space
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	n = 48
	if (n < 58)
	{
	putchar(n);
	while (n != 57)
	putchar(',');
	putchar(' ');
	}
	putchar('\n');
	return (0);
}
